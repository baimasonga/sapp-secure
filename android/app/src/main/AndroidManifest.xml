<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Salone Shield requests the minimum set of permissions.
         INTERNET is only used for optional Supabase sign-in and threat reporting;
         all message analysis runs on the device. No contacts, SMS, storage or
         accessibility permissions are declared in this milestone. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- POST_NOTIFICATIONS lets the app tell you that a message which just
         arrived is worth checking. The notification carries no message text.
         Notification *access* (reading other apps' notifications) is not a
         uses-permission: it is the BIND_NOTIFICATION_LISTENER_SERVICE guard on
         the service below, which only the system can bind to and which the
         user must switch on themselves in Android settings. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="Salone Shield"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Share-to-Salone-Shield: receive plain text shared from WhatsApp
                 or any other app. No permission is required for this. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <!-- Reads messaging notifications so a scam can be caught as it
             arrives. Only the system may bind to it, and only after the user
             grants notification access in Android's own settings — the app
             cannot grant this to itself. exported="true" is required for the
             system to bind it at all; what keeps everyone else out is the
             bind permission below, which is signature-level and held only by
             the system. -->
        <service
            android:name=".NotificationSecurityService"
            android:label="@string/notification_listener_label"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
